從2025/5/25開始記錄
-----------------------------------------------------------------------------------------------
AV_V3.1 2025/5/25

做了：
1.拼命的搞出了draw_array_heap
2.增加了draw_array的range與data_type等參數
3.把高光的線條調整成邊緣也會的形式
4.把AV.hpp各種參數都加進去了
5.把動畫最快速度改成1ms 並且做了1000個heap插入的壓力測試 測試結果:沒有卡頓出現 非常絲滑 我還挺滿意的

發現的BUG：
1.程式碼在伸縮後縮排會怪怪的                                                                            (已修正)
2.錯誤訊息應該只出現在debug_log 卻出現在output                                                          (已修正)
3.itemsPerRow <= 0 的情況畫出來會怪怪的                                                                (已修正)

想做：
1.draw_array的改變陣列格子的背景顏色的參數 這樣更方便呈現某些效果 最好是要多少顏色和格子都可以自訂           (已完成)
2.播放動畫時 同時可以將播放到的程式碼區塊高光                                                            (已完成)
3.新增draw_array_BIT的data_type                                                                       (已完成)
4.希望做一個可以畫箭頭、方塊、圖形等等的插件小工具 這樣不用在draw_array上搞太多雜工 還可以方便呈現一些效果    (已完成)
5.AV.hpp 的參數自訂化 並且加上template MEGA_PROGRAMMING!!!                                             (已完成)

-----------------------------------------------------------------------------------------------
AV_V3.2 2025/5/30

做了：
1.修正了上次的BUG
2.新增了 focus 的陣列參數 可以方便將注意力集中在標起來的索引值上 沒被標到的值會暗掉
3.做了一個 heap.cpp , 埃篩 , 線篩 的展示樣本

發現的BUG：
1.加了 lineGap 後 highlight會沒連結 醜醜的 我在想要不要乾脆刪掉 lineGap 這個參數=w=

想做：
1.新增draw_array_BIT的data_type (目前迫切想做的)                                                       (已完成)
2.draw_2Darray 畫二維陣列 或是 draw_data 畫一個變數(可bit化)
3.既然數字都要bit化 那陣列也要多一個 draw_type 叫做 bitwise 並且可以做到 0和1 分別用黑白呈現
4.timeline以及DFS tree的展現

-----------------------------------------------------------------------------------------------
AV_V3.3 2025/6/10

做了：
1.意外的輕鬆地 搞出了draw_array_BIT 畫起來還挺酷的w
2.修正了群組位移會有偏差的 bug
3.新增了 index = 2 的設定 可以只把索引值畫出來了
4.做了個簡單的前綴和範例 (做一半) 但是我覺得還能更好
5.新增了 Binary Indexed Tree 的視覺化程式範例
6.在 AV.hpp 裡面新增了一個 AtoB 函數 他可以直接回傳一個從A~B的遞增陣列回來 方便程式撰寫

發現的BUG：
1.在offset非0的情況下 位移群組會有偏移的問題                                                            (已修正)

想做：
1.可以自訂化格子的背景顏色                                                                              (已完成)
2.新增 BIT 的視覺化程式範例                                                                            (已完成)
3.讓draw_array有各式各樣的標記工具 (箭頭、打勾、右下斜角標記、浮出個子等等)                                (已完成)
4.需要有的小步與大步的幀數區別 應該會做成兩條軌道 並且分開用指令執行                                       (已完成)

-----------------------------------------------------------------------------------------------
AV_V3.4 2025/6/14

做了：
1.做了key_frame的結構設計 可以一次看大部分的演算法情況 也可以看細節
2.試著在線篩範例中插入key_frame (效果還行 但感覺還差了點甚麼)
3.重新整理了一下draw_array的整個邏輯 把draw_array_outerframe移到 draw_array_normal,BIT,heap 才畫
4.新增了 point, mark, modify 的畫圖形式 分別表示 當前箭頭、已經處理完的、當前改變的
5.修改了 埃篩及線篩的範例 並加入了上述的畫圖形式來描述它

想做：
1.加入一個顯示幀數的元件 可以看到哪裡有關鍵幀 點下去可以跳到該幀
2.在draw_array_heap 底下新增一個可以改區段顏色的設定 (線段樹)                                             (已完成)

-----------------------------------------------------------------------------------------------
AV_V3.5 2025/9/16

做了：
1.Segment_Tree 的展示樣本
2.做了draw_array_segment_tree.js 的畫圖形式 專門來展示線段樹 多了一個畫區段顏色的設計

想做：
1.要把sets也給加進segment_tree的設定                                                                     (已完成)
2.其他種segment_tree 需要我的根說明他的分度
-----------------------------------------------------------------------------------------------
AV_V3.6 2025/10/16

做了：
1.更改了 Segemnt_Tree 的展示樣本(把它tree,lazy合併成一顆)
2.大改了style的傳輸結構 我把所有 highlight,focus,... 的屬性全部整合成一個style屬性 這樣以後不用再東改西改的了 但是也衍伸了問題3
3.做了 draw_array_segment_tree 中可以只染色字體本身或是字體背景的設定 並且可以只染一個區段 但是做的還不完全 因為C++資料結構傳輸是一個困難的問題
4.做了一個 CDVS 的 style 只要把計數過後的陣列套上這個屬性就可以畫出 哪些地方動特別多次那它的顏色就會更深

-----------------------------------------------------------------------------------------------
AV_V3.7 2025/10/28

做了：
1.把所有自訂化顏色都大概實裝上了(Segemnt_Tree)
2.把 Segemnt_Tree 的展示樣本加了 sets 進去
3.額外開了兩個參數為了解決上次的問題3

想做：
1.把大部分style元素重作一下 並使其可加入一些特效效果

-----------------------------------------------------------------------------------------------
AV_V3.8 2025/11/5

做了：
1.把輸入、輸出、debuglog區塊分開了
2.美化了一下按鈕
3.讓區段染色可以改顏色
4.加了 draw_text.js 可以顯示一個對話框裡面有文字

想做：
1.對話框的位置有點麻煩 希望可以改成用相對位置的方式
-----------------------------------------------------------------------------------------------
AV_V3.9 2025/11/12

做了：
1.修改了一下 segment_tree 的範例 讓他可以有文字說明了
2.做了可以讓 //draw 自動全部隱藏的按鈕
3.加了 TTS 和播放聲音的按鈕 但好吵...
-----------------------------------------------------------------------------------------------
AV_V3.10 2025/11/19

做了：
1.修改了 draw_normal.js 把它改成可以正常改顏色了
2.修改了 線篩 的範例
3.新增了一個 drawColoredText 可以輸出有顏色的字體了
-----------------------------------------------------------------------------------------------
AV_V3.11 2025/11/22

做了：
1.新增了 av.stop() 的斷點功能 跳到最後會停在下一個斷點幀
2.新增了 av.faston() 的加快幀 只要路過它之後的自動撥放就會以一大步一大步播放
3.新增了 av.skip() 的省略幀 只要碰到他就直接跳到下一個停止幀
4.修改 TTS 的朗讀邏輯
5.修改 drawText 加了一個括號判斷，只要是 {:} 就是畫前面讀後面，是 {} 就是只畫前面，是() 就是包含括號一起畫 但不會讀出來
6.新增了最上面的列表設定
7.新增了程式碼高亮
8.大修了 draw_array 底下各個程式的設計 現在看起來乾淨多了
9.新增了 draw_block.js 可以直接呼叫來畫一個格子了
10.新增了 draw_array_hintWidget.js 來放各種提示元件
11.修改了所有的範例 使其符合目前功能

發現的BUG：
1.第一次執行時 自動縮字體會失效 但重新整理就正常了
2.text 的 大括號裡面的 冒號和空白 會消失                                                                   (已修正)
3.color_text 有時候文字會擠在一起                                                                         (已修正)
-----------------------------------------------------------------------------------------------
AV_V3.12 2025/12/6

做了：
1.新增了簡易版的線段樹範例
2.修改了線篩的範例
3.新增了 draw_2Darray.js 來畫二維陣列
4.新增了 draw_arrow.js 來畫箭頭
5.把目前所有的物件都加上相對位置轉絕對位置的函式
6.新增了動畫漸變的效果 看起來會比較絲滑一點
7.將 AV.hpp 大部分需要用到的函式都加上了 mega_programming 現在可以傳各種參數了
8.因為上面這條 現在線段樹可以顯示A+B了 說不定就不需要再多傳一個lazy, sets了

想做：
1.將 2Darray 的功能完善                                                                                   (已完成)
2.MEGA_PROGRAMMING!!!                                                                                    (已完成)
3.新增一個可以利用相對位置取得絕對位置的函式                                                                 (已完成)
4.新增一個可以畫箭頭的物件                                                                                 (已完成)

發現的BUG：
1.TLE判斷不見了                                                                                           (已修正)
2.格線不見了                                                                                              (已修正)
3.下一步按到最後 有時候顏色動畫會卡在一半
4.skip 有時候會沒有跳過 因為走一大步會直接跳過他的那幀
-----------------------------------------------------------------------------------------------
AV_V3.12 2025/12/15

做了：
1.現在可以取得一個group的位置 並將線段畫在其上下左右 這樣之後的樹應該就畫比較好畫了
2.完善了顏色擴散問題 color_diffusion_problem 的程式視覺化範例
3.完成了LCS的演算法視覺化範例

發現的BUG：
1.按兩次RUN會 TLE                                                                                         (已修正)
-----------------------------------------------------------------------------------------------
AV_V3.12 2025/12/22

想做：
1.修改動畫變換規則 只重畫要變色的格子
2.新增堆疊作畫的模式 畫DFS最好這樣子畫 不然太複雜
-----------------------------------------------------------------------------------------------
AV_V3.13 2026/01/12

做了：
1.完成了伺服器的基本設定
2.加入 nginx 來取得靜態檔案
3.加入 docker-compose.yml 的伺服器設定檔
4.小改前端 加入演算法範例清單
5.加入登入的彈出視窗

發現的BUG：
1.按播放後 再按兩次撥放聲音會直接跳到下一幀                                                                  (已修正)
-----------------------------------------------------------------------------------------------
AV_V3.14 2026/01/16

做了：
1.完成了登入的部分
2.完成了儲存程式碼的部分

-----------------------------------------------------------------------------------------------
AV_V3.15 2026/02/10

做了：

想做：
1.完成二維陣列的getposition
2.讓物件只要有綁定相對位置 在操作後都會跟著相對位置移動
3.開啟無刪除模式來畫遞迴的東西
4.做背包問題和編輯距離的演算法範例
5.寫一個draw指令的使用說明書